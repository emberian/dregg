# DREGGNET MIGRATION LEDGER — ./dreggnet slop → dregg-native breadstuffs

*Fable · 2026-07-03 · read-only audit `wf_f97362fa-5a0` (6 theme lanes + synthesis).*
*`./dreggnet` (and `~/dev/DreggNet`) is the retired proprietary DreggNet. It is being
**ABANDONED** — not maintained, not rejoined, no future weld. It is a **reference to
strip-mine**: take the logic, rewrite it dregg-native in the main workspace against OUR
cells, delete the source. There is no "seam back" and the repos will never meet.*

*Status per subdir: **MIGRATED** (native equivalent already exists → delete the slop) ·
**UNIQUE** (real logic with no native home yet → strip-mine + port, then delete) ·
**DEAD** (stale/DreggNet-infra-specific → just delete). "native home" = where it lives
or should be built HERE; DreggNet paths are only cited to say what to strip-mine from.*

Ground truth confirmed: 31 code subdirs + build-infra files on disk; all cited native exemplars and `starbridge-apps/*` targets exist. The two catalogue passes (the 5 theme passes + the coarser `misc-remainder` census) cover the same physical subdirs, so I've collapsed them to one row each, preferring the deeper theme analysis and flagging where `misc-remainder` diverged.

Here is the migration ledger (ready to become `dreggnet/DREGGNET-MIGRATION.md`):

---

# DREGGNET-MIGRATION.md — the one migration ledger

Scope: `/Users/ember/dev/breadstuffs/.claude/worktrees/deos-night-shift/dreggnet` (31 code subdirs + build infra). Every row is one physical subdir. Statuses: **MIGRATED** (native equivalent exists, delete the dregg copy), **DEAD** (targets the retiring deployed stack, delete, no port), **UNIQUE** (no native home yet — the migration backlog).

Native tree root for all `home` paths: `/Users/ember/dev/breadstuffs/.claude/worktrees/deos-night-shift/`.

## 1. Summary table

| Subdir | Status | Native home / port target | Priority |
|---|---|---|---|
| `control` | **UNIQUE** (economic+durable halves migrated) | `starbridge-apps/execution-lease` + `compute-exchange`; UNIQUE remainder = operational provisioning/mesh adapter | **high** |
| `exec` | **UNIQUE** (mostly thin over native) | owned-wasmi sandbox → new dregg-native sandbox crate / module in `deos-hermes` | **high** |
| `console` | **UNIQUE** | `deos-view` applet (`deos-view/src/web.rs`,`render.rs`) + `dregg-auth` scoping | **high** |
| `billing` | **UNIQUE** | new `starbridge-apps/billing` cell (`cell/src/obligation_standing.rs` + `allowance.rs`) | **high** |
| `org` | **UNIQUE** | new `starbridge-apps/org` cell (`dregg-auth/src/grant.rs` attenuation; pattern `tool-access-delegation`) | **high** |
| `agent-host` | **UNIQUE** (confined run is native) | edge-identity map → `dregg_cell` mandate cell; `authorized_keys` gen = thin adapter | medium |
| `guard` | **UNIQUE** (rate/quota native) | new `starbridge-apps/guard` (`cell/src/allowance.rs` + `tool-access-delegation`; standing via `governed-namespace`) | medium |
| `dregg-secrets` | **UNIQUE** (store partly native) | extend `secrets/` OR new `starbridge-apps/managed-secrets` (`dregg-auth` gate) | medium |
| `dregg-deploy` | **UNIQUE** (blocked on `exec`) | new `starbridge-apps/site-deploy` (mirrors `execution-lease`); **NAME COLLIDES** w/ main `dregg-deploy`=DreggDL | medium |
| `dregg-domains` | **UNIQUE** | new `starbridge-apps/` FactoryDescriptor cell (pattern `nameservice`); injected `DnsResolver` | medium |
| `webapp` | **UNIQUE** (lease half migrated) | `execution-lease` (run) + `deos-web-cells`/`starbridge-web-surface::web_of_cells` (serve app as cell) | medium |
| `gateway` | **UNIQUE** (only fly-compat is new) | `execution-lease` + `deos-web-cells`; keep thin fly→lease adapter *iff* fly-compat is a requirement | medium |
| `dreggnet-logs` | **UNIQUE** | extend `observability/` with per-tenant durable sink; tail/follow/search = cap-scoped read turns | medium |
| `umem` | **UNIQUE** (core native, thin wrapper is the delta) | small `dregg-umem` helper / module in `cell/` over `cell/src/state.rs:429` `compute_heap_root` + `dregg_merge::GrowSet` | low |
| `dregg-ipfs` | **UNIQUE** | transport module in `storage/` (`content.rs`+`commitment.rs`); injected `IpfsClient` seam | low |
| `sandstorm-bridge` | **UNIQUE** | small `.spk`-verify crate + grain-as-cell over `cell/`; powerbox→`dregg-agent/src/grant.rs` caps | low |
| `http` | **UNIQUE → DEAD-adjacent** | no native home (native model hand-rolls no HTTP/1.1); dies with its 4 consumers | low |
| `status` | **UNIQUE** | read-only view over `node/` federation health turns + `/status.json` projection (`deos-leptos`/`site`) | low |
| `cli` | **DEAD** (crate) | delete crate; carry lease/run/deploy/domains/login verbs to main `cli/` as spec *after* backends native | low |
| `attach` | **MIGRATED** | `dregg-agent/src/{session,live,session_store}.rs`; desktop face `starbridge-v2/src/deos_desktop/*` | — |
| `bridge` | **MIGRATED** | `starbridge-apps/execution-lease/src/{lib,service}.rs` + `compute-exchange` | — |
| `durable` | **MIGRATED** (delete-after-dep) | `pg-dregg/src/workflow.rs` + `starbridge-apps/agent-orchestration`; delete once `exec` run_workload lands | — |
| `landing` | **MIGRATED** | `site/root/index.html` + `site/assets/style.css` (Caddy static) | — |
| `receipt` | **MIGRATED** (delete-after-dep) | `turn/src/turn.rs` `TurnReceipt` + `cell-crypto/src/note_bridge.rs` `BridgeReceipt`; delete once billing/guard/org emit real turn receipts | — |
| `storage` | **MIGRATED** | `storage/` + `dregg-storage-templates/` + `starbridge-apps/storage-gateway-mandate` | — |
| `webauth` | **MIGRATED** (delete `cred.rs`) | `dregg-auth/src/credential/{chain,caveat,pred,wire}.rs`; forward-auth shim = legacy-deploy artifact | — |
| `demo` | **DEAD** | throwaway demo scripts | — |
| `deploy` | **DEAD** | `/fulfill` semantics captured by `starbridge-apps/execution-lease` | — |
| `ops` | **DEAD** | superseded by the cell-based management console (DREGG-COMPUTER.md); **not** `observability/` (collision) | — |
| `runbooks` | **DEAD** | prose only; salvage KEY-MANAGEMENT/DR/INCIDENT to `docs/` optionally | — |
| `scripts` | **DEAD** | `scripts/test-gauntlet.sh` + `ship-readiness-smoke.sh` | — |
| `Makefile`,`Dockerfile`,`docker-compose.yml` | **DEAD** | main `docker/` + `scripts/` | — |

Collision guards (verified against root `Cargo.toml`): main-workspace `dregg-deploy`(DreggDL), `secrets/`(local AES store), `cli/`(federation client `dregg`), `observability/`(trace emitter) are DIFFERENT crates from the same-named dreggnet ones — name match ≠ MIGRATED.

## 2. DELETE-NOW list (MIGRATED + DEAD)

**Delete immediately (native equivalent live and complete):**

- `attach/` → superseded by `dregg-agent/src/session.rs` + `live.rs` (`LiveRun`/`verify_live`) + `session_store.rs`.
- `bridge/` → superseded by `starbridge-apps/execution-lease/src/{lib,service}.rs` (+ `compute-exchange`).
- `landing/` → superseded by static `site/root/index.html` (+ `site/assets/style.css`); fold `LandingConfig` pitch copy + `/status.json` banner into that page first.
- `storage/` → superseded by `storage/` + `dregg-storage-templates/` + `starbridge-apps/storage-gateway-mandate` (only a tenant bucket-API veneer would re-home).
- `webauth/src/cred.rs` → byte-for-byte port of `dregg-auth::credential`; delete and depend on `dregg-auth`. (The `/auth` server + `grant.rs` host→cap map is a Caddy legacy-deploy shim — retire with the Caddy-gated ops surfaces, don't port.)
- `demo/`, `deploy/`, `ops/`, `runbooks/`, `scripts/`, `Makefile`, `Dockerfile`, `docker-compose.yml` → DEAD deployment/build infra for the retiring stack. `deploy//fulfill` is already `execution-lease`; `ops` is superseded by the DREGG-COMPUTER.md cell console (NOT by `observability/`); `scripts`/`Makefile`/`docker` by main `scripts/` + `docker/`.
- `cli/` → delete the crate (thin weld, no logic); it is NOT the main `cli/`. Keep the verb surface (lease open/run/status, deploy, domains, logs, login) as a porting spec for subcommands added to main `cli/` **after** the backends are native.

**Delete-after-dependency (native equivalent exists; one consumer must move first):**

- `durable/` → `pg-dregg/src/workflow.rs` + `starbridge-apps/agent-orchestration` supersede it *more strongly* (every step is an AUTHZ→CHAIN→APPLY+LOG verified turn). Delete once `exec`'s owned-wasmi `run_workload` step lands natively (the only thing binding this to a sandbox workload).
- `receipt/` → `turn/src/turn.rs` `TurnReceipt` (proven tamper-evident, `metatheory/…/Receipt.lean`) + `note_bridge.rs` `BridgeReceipt`. Delete once `billing`/`guard`/`org` emit real turn receipts instead of the `dreggnet-receipt` shim.
- `http/` → no native equivalent by design (native web = web-of-cells + `deos-view` HTML + Caddy static). It is DEAD-adjacent: delete together with `gateway`/`console`/`landing`/`webauth` serving loops. If a traditional edge is deliberately kept, fold its 4 files into the surviving fly-adapter as a private module.

## 3. PORT list (UNIQUE) — the migration backlog, ranked

### HIGH

1. **`exec` → dregg-native sandbox** *(unblocks `durable`, `webapp`, `dregg-deploy`)*
   Almost all thin over native already: `budget.rs`/`meter.rs` re-export `dregg-agent`; `agent_toolkit.rs` (154 L) is cloud wiring over `dregg_agent::toolkit`; `egress.rs` (1002 L) twins `deos-hermes/src/egress.rs`+`confined.rs`; `capture.rs`→`dreggnet_logs`; `model.rs`→shared Meter.
   *Smallest shape:* one crate (or module in `deos-hermes`) exposing `run_workload(tier, module, input)` = the owned **empty-Linker fail-closed wasmi engine** in `lib.rs` (762 L) + the `CapTier` tier-gate with `TierNotServed`/`NotWired` seams, driven from the confined-agent gate so every run is cap-gated + metered + receipted. `dregg-agent`'s `toolkit` already expects this injected (`with_run_workload`).
   *Target:* `deos-hermes` (module) or new `dregg-sandbox` crate.

2. **`control` → the vat** *(see headline §4)*
   *Smallest shape:* a thin operational adapter crate (`ec2.rs`, `server.rs`, `node_api.rs`, `mesh.rs`, `wg.rs`, `scheduler.rs`, `fleet.rs`, `orchestrator.rs`, `local.rs`) that the vat drives — with every provision/place/lapse/reap state transition moved onto verified turns against an `execution-lease` cell.
   *Target:* `starbridge-apps/execution-lease` + `compute-exchange` (money+lifecycle) + a `dreggnet-fleet-adapter` op crate.

3. **`console` → deos-view applet** *(named product gap, DREGG-COMPUTER.md/WEB-PORTALS-CENSUS.md "no unified console")*
   *Smallest shape:* one `deos-view` applet crate — a vstack of the subject's cells, each row a `bind()` over the live ledger, each action a cap-gated verified turn; cap-scoped by the `dregg-auth` caveat-chain subject (not `console/src/scope.rs`'s fixture catalog). Rendered via `deos-view/src/web.rs` (browser) or `render.rs` (cockpit). Drop the bespoke HTTP server.
   *Target:* new `deos-view` applet crate + a `Catalog` source bound to real read surfaces.

4. **`billing` → starbridge-apps/billing**
   No new primitives. Invoice = aggregation VIEW over settled turn receipts (each line item carries its settle-receipt hash), sealed as its own turn receipt. Spend cap = a `cell/src/allowance.rs` ceiling cell (over-cap charge refused by the executor = the 402 shape). Estimate = pure fn over the rate card. Recurring half → `cell/src/obligation_standing.rs`.
   *Verified turns:* `assemble-invoice-for-period`, `charge-under-cap`. *Target:* new `starbridge-apps/billing` (sibling of `subscription`/`escrow-market`).

5. **`org` → starbridge-apps/org**
   An Org factory cell owning member→role slots (`SetField`/`WriteOnce`); a role-cap = an org-owner `dregg-auth` grant attenuated to the role's permission set — "viewer read-only" / "admin can't delete org" is the SAME no-amplify lattice `dregg-auth/src/grant.rs` already proves. Org-scope + role-gate = cap-context caveats checked by the executor.
   *Verified turns:* `invite`, `accept` (mints member role-cap), `remove`, `change-role`, `transfer-ownership`. *Target:* new `starbridge-apps/org` (pattern `tool-access-delegation` + `governed-namespace`).

### MEDIUM

6. **`agent-host`** — port the EDGE IDENTITY MAP in `lib.rs` (636 L): SSH pubkey→`dga1_` account→budget→cap bundle → a `dregg_cell` mandate cell (same Mandate attenuation `agent-orchestration` proves: granted⊆held, sub-budget, scoped tool-set). `authorized_keys` forced-command generator stays a thin deploy adapter reading that cell. `isolation.rs` is unwired scaffolding — drop unless doing real OS-boundary work. *Target:* `dregg-agent` + deploy adapter.

7. **`guard`** — compose native, don't hand-roll: per-subject quota/rate = `cell/src/allowance.rs` ceiling cells (over-ceiling refused in-band = 429/402) reusing `tool-access-delegation`'s verified counter+ceiling; account standing = a `SetField` slot moved only by a receipted governance turn (takedown/suspension). Only the abuse-governance/standing layer is new. *Target:* new `starbridge-apps/guard` (pattern `governed-namespace`).

8. **`dregg-secrets`** — three unique parts: (1) injection seam `inject.rs` (~256 L) — cap-gated `inject_for_workload` → redacted-Debug `InjectedSecrets`, fail-closed (belongs beside the native exec/sandbox seam; generalizes the BYO-LLM-key brain-pocket confinement in `deos-hermes` resident.rs); (2) receipted audit `audit.rs` → secret reads as verified turns on an access-log cell; (3) `kms.rs` key hierarchy gated by `dregg-auth`. Storage reuses/extends `secrets/`. Honest limit: operator-held KMS root can still decrypt — true operator-blindness needs a tenant-held root. *Target:* extend `secrets/` or new `starbridge-apps/managed-secrets`.

9. **`dregg-deploy`** *(blocked on `exec`)* — new `starbridge-apps/site-deploy` mirroring `execution-lease`'s shape. Deploy = factory-born durable cell, `Clone→Build→Publish` = verified turns; metered build lease = `obligation_standing` + `payable.rs` Transfer; build image = committed umem cell-heap checkpoint; Publish = SiteCell + receipt carrying the cloned commit hash. Heavy: the cap-bounded build sandbox needs `exec`'s CapTier ported first. *NAME COLLISION:* main `dregg-deploy` = DreggDL, unrelated. *Target:* new `starbridge-apps/site-deploy`.

10. **`dregg-domains`** — new `starbridge-apps/` FactoryDescriptor cell, slots `{domain, site, owner, verification_state, challenge_nonce}`; `DomainCap`-gated `bind` turn + `verify` turn (`WriteOnce(challenge_nonce)`, `Monotonic(verified_seq)`); `DnsResolver` injected seam (MockDns in tests). `site_for_host`/`is_verified` = verified reads the gateway consults. Pattern `nameservice` (DNS-challenge BYO ≠ federation names). *Target:* new `starbridge-apps/` cell.

11. **`webapp`** — lease/meter/durable-image half is DONE in `execution-lease` (`LeasedRouter`→per-period obligation + Payable). Remaining: route→owned-wasm handler becomes a `deos-web-cells` `WebBundle` (`publish/fetch/rehydrate`) or a `deos-view` applet cell — the "app" is a published cell served through the membrane. Drop `spec.rs`/`router.rs`/`http.rs`. *Target:* small crate composing `execution-lease` + `deos-web-cells`.

12. **`gateway`** — only genuinely-unique piece is the **fly.io Machines HTTP compat surface** (`route.rs`+`lease.rs`+`api.rs`) — a thin adapter binding fly routes → `execution-lease` turns (no native fly-Machines API exists anywhere; grep confirmed). Other handlers migrated: `SiteHostHandler`/`sitepublish`→`WebOfCells::publish/fetch`; `WebAppHandler`→#11; webauth gating→`dregg-auth`. Keep only the fly→lease adapter *iff* fly-compat is a product requirement; else the edge collapses into web-of-cells + Caddy + execution-lease and retires. *Target:* small `dregg-fly-adapter` or retire.

13. **`dreggnet-logs`** — extend `observability/` with a per-tenant durable sink; tail/follow/search = cap-scoped read turns over that sink (re-witnessable against the tenant cell). *Target:* `observability/`.

### LOW

14. **`umem`** — CORE IS ALREADY NATIVE (the boundary root is the real kernel sorted-Poseidon2 `dregg_cell::compute_heap_root` at `cell/src/state.rs:429`, pinned by `root_binds_get`; `CellState` is the heap). Delta = a thin REUSABLE wrapper (record-laying convention + fork/checkpoint/restore/time-travel) that `execution-lease` currently reimplements inline and `control/compute_cell.rs` already treats as "the ONE primitive both lanes wrap." *Smallest shape:* a small `dregg-umem` helper (or module in `cell/`) exposing `lay/open/boundary_root/fork/checkpoint/restore` over `CellState` + `compute_heap_root`, verified against `root_binds_get` + `dregg_merge::GrowSet`; then execution-lease and the vat both depend on it. (`misc-remainder` marked this MIGRATED — correct about the core, but the reusable wrapper is a real, tiny port.)

15. **`dregg-ipfs`** — transport-only module in `storage/`: CIDv1 encode/decode over the existing blake3 commitment (`Cid::from_blake3_digest`, no re-hash) + injected `IpfsClient`/`HttpPost` seam (MockIpfs / Kubo `KuboClient`). No verified turns — the commitment IS the address. *Target:* `storage/`.

16. **`sandstorm-bridge`** — small crate for `.spk` verify + `manifest→GrainSpec` (pure, no net); grain lifecycle = a cell over `cell/` with `/var` folded into the state commitment; powerbox grant = strictly-attenuating witnessed turn reusing `dregg-agent/src/grant.rs`. Port last. *Target:* integration crate over native exec + cap surfaces.

17. **`status`** — read-only view over `node/` federation health turns + a `/status.json` projection; no state of its own (public subset of `ops`). *Target:* `deos-leptos`/`site` web tree.

18. **`http`** — see §2. Port only if a traditional HTTP edge is deliberately retained (fold into the surviving fly-adapter as a private module); otherwise it dies with its consumers.

## 4. Headline — the vat substrate

**Is `control`/ServerFleet MIGRATED or UNIQUE? → SPLIT, and net UNIQUE — but only its operational plane is genuinely unique. Its money, its lifecycle-durability, and its durability primitive are already native.**

- **Already MIGRATED (delete/fold):** the ECONOMIC rail — `settle_ledger.rs`/`hosting_meter.rs` (per-`(lease,period)` exactly-once settlement + per-period uptime metering) is exactly what `starbridge-apps/execution-lease/src/lib.rs` models as a `StandingObligation` + `Payable Effect::Transfer` on a lease cell; the post/bid/settle market is `starbridge-apps/compute-exchange/src/lib.rs`. `control/compute_cell.rs` (server-as-umem-cell: sleep=checkpoint/wake=restore/scale=fork) IS the execution-lease durable-image cell (EXEC_COLL heap over `dregg_cell::compute_heap_root`) and should fold into it. The durability primitive underneath (`umem`) is native (§3.14).
- **Genuinely UNIQUE, no native home:** the OPERATIONAL PROVISIONING GLUE — `ec2.rs` (1141 L, aws-CLI argv/lifecycle), `server.rs` (2050 L), `node_api.rs` (1285 L), `mesh.rs` (1198 L, Tailscale overlay), `wg.rs`, `scheduler.rs` (place→fulfill→reap), `fleet.rs` (health/pick/failover), `orchestrator.rs`, `local.rs`. (Note: the assigned `lease`/`net` subdirs do NOT exist under `dreggnet` — lease = the migrated `execution-lease`; networking lives inside `control`'s `mesh.rs`/`wg.rs`.)

**Shortest path to a native `starbridge-apps/vat`:**

1. **Land `exec`'s owned-wasmi `run_workload` natively** (§3.1) — a dregg-native sandbox with the empty-Linker floor + CapTier seams, cap-gated/metered/receipted from the confined-agent gate. This unblocks `durable`, `webapp`, `dregg-deploy` too.
2. **Fold `compute_cell.rs` into `execution-lease`** — delete the inline server-as-cell reimplementation; use the EXEC_COLL durable-image cell.
3. **Extract the `umem` helper** (§3.14) so `execution-lease` and the vat share `lay/open/boundary_root/fork/checkpoint/restore` over `dregg_cell::CellState` instead of hand-rolling.
4. **Move every lifecycle/authorization/settlement state transition** (provision → place → lapse → reap, + settle/meter) onto **verified turns against an `execution-lease` cell** — so a light client can witness the fleet. The *placement decision* stays imperative; the *money and the lifecycle* become cells.
5. **Keep provisioning + mesh** (`ec2`/`server`/`node_api`/`mesh`/`wg`/`scheduler`/`fleet`/`orchestrator`/`local`) as a **thin operational adapter crate the vat drives** — the driver of the cells, not the source of truth.

**Result:** `starbridge-apps/vat` = an `execution-lease`-backed ServerFleet cell (lifecycle + settlement as witnessed turns) + a thin `dreggnet-fleet-adapter` operational crate + the shared `dregg-umem` helper. **No new verified primitive is required** — it composes `execution-lease` + `compute-exchange` + the ported `exec` sandbox + the `umem` helper. The critical-path blocker is step 1 (`exec`); everything else is fold-in and turn-ification of state already modeled natively.

---

## Per-theme catalogue (condensed)

### vat-substrate — the Dregg Computer's compute plane: the ServerFleet vat (provision/schedule/mesh/settle), the owned wasm execution sandbox, DBOS-style durable workflows, the SSH-attach hosting edge, and the umem-cell durability primitive under all of them. Note: the assigned `lease` and `net` subdirs do NOT exist under ./dreggnet — lease functionality is the already-migrated starbridge-apps/execution-lease, and networking lives inside control (mesh.rs/wg.rs). Verified against the migrated exemplars (starbridge-apps/execution-lease, compute-exchange) and tonight's native surfaces (starbridge-v2 durable_desktop.rs / resident_agent.rs Hireling / agent_room.rs, pg-dregg/workflow.rs, deos-hermes egress+confined, cell/state.rs compute_heap_root).
- **control** `[UNIQUE]` · high — dreggnet-control: the ServerFleet vat — VmProvider (LocalProvider via bridge + Ec2Provider over the aws CLI), Scheduler place->fulfill->reap, multi-backend fleet health/pick/failover, Tailscale/WireGuard overlay mesh dis
    - home: starbridge-apps/execution-lease + starbridge-apps/compute-exchange (economic layer already native); compute_cell.rs already delegates to dreggnet_umem::UmemCell
    - port: Split: the ECONOMIC surface is already MIGRATED — settle_ledger.rs / hosting_meter.rs (per-(lease,period) exactly-once settlement, per-period uptime metering) is what starbridge-apps/execution-lease/src/lib.rs models as a StandingObligation + Payable Effect::Transfer on a lease cell, and the post/bid/settle market is starbridge-apps/compute-exchange/src/lib.rs (AffineEq conserving settle). compute_cell.rs (server-as-umem-cell, sleep=checkpoint/wake=restore/scale=fork) IS the execution-lease dura
- **exec** `[UNIQUE]` · high — dreggnet-exec: DreggNet's execution surface — run_workload runs a dregg-authorized workload at the CapTier the lease grants; the Sandboxed tier is an OWNED zero-unsafe wasmi interpreter against an EMPTY Linker (total-den
    - home: deos-hermes (confined-agent rail: src/confined.rs + src/egress.rs + src/host.rs); budget/meter already extracted to dregg-agent; agent_toolkit already thin over dregg_agent::toolkit
    - port: Most of exec is ALREADY thin over native: budget.rs/meter.rs are re-exported from dregg-agent (EXTRACTED, docs/AGENT-RUNTIME-OPEN-SOURCE.md); agent_toolkit.rs (154 lines) is cloud wiring over dregg_agent::toolkit; egress.rs (1002 lines, deny-by-default EgressPolicy) has a native twin in deos-hermes/src/egress.rs (EgressGrant/EgressPolicy::sealed) + confined.rs (SBPL/Landlock jail); capture.rs writes into dreggnet_logs; model.rs lowers onto the shared Meter/cap/receipt. The ONE genuinely UNIQUE, 
- **durable** `[MIGRATED]` · low — dreggnet-durable: DBOS/duroxide-style durable, transactional, crash-recoverable workflows over owned-sandbox execution — an ORCHESTRATION whose steps are run_workload activities each twinned with a meter-tick, checkpoint
    - home: pg-dregg/src/workflow.rs (durable workflows where every step is a verified turn) + starbridge-apps/agent-orchestration/src/lib.rs (durable + auditable, exactly-once, modeled on pg-dregg run_durable/recover_durable/resume_durable)
    - port: The durability MECHANISM is superseded natively and more strongly: pg-dregg/src/workflow.rs realizes the identical DBOS shape ('DBOS, but every step is a verified turn') but each step passes the AUTHZ->CHAIN->APPLY+LOG spine so a step cannot forge value (the exact bug plain-DBOS UPDATEs allow), and starbridge-apps/agent-orchestration wraps it as a crash-recoverable, light-client-auditable receipt chain. The Postgres transactional-outbox meter-tick (charge <=> durable step, idempotent) is the sam
- **agent-host** `[UNIQUE]` · medium — dreggnet-agent-host: the SSH-attach hosting edge — an AccountRecord binds an SSH pubkey to a dga1_ cap-account + budget + cap bundle, and AgentHostRegistry emits OpenSSH authorized_keys forced-command lines that drop eac
    - home: dregg-agent (session/cred/attach) + deos-hermes (HermesGateway confinement) + starbridge-v2/src/resident_agent.rs (the Hireling weld) + starbridge-v2/src/agent_room.rs (Agent Room); the confined session it fronts is already native
    - port: The confined RUN is fully native now: dregg-agent owns the attach loop, deos-hermes routes every tool-call through the cap-gated/metered/receipted ToolGateway, and tonight's Hireling (resident_agent.rs hire_resident, mirror-welds real verified turns onto the live World) + Agent Room surface it. The genuinely UNIQUE remainder is the EDGE IDENTITY MAPPING in lib.rs (636 lines): SSH pubkey -> dga1_ account -> budget -> cap bundle, validated against the real grant vocabulary at enrol, persisted, and
- **umem** `[UNIQUE]` · low — dreggnet-umem: the umem-cell durability primitive under the whole vat — UmemCell + UmemRegistry lay records as length-delimited leaves into a dregg_cell heap, seal the kernel Poseidon2 boundary_root, durably materialize/
    - home: cell (dregg_cell): the substrate already lives at cell/src/state.rs::compute_heap_root + CellState; native apps (starbridge-apps/execution-lease EXEC_COLL, starbridge-apps/nameservice) already lay records into a dregg_cell heap directly
    - port: The CORE is already native — the boundary root is the real kernel sorted-Poseidon2 dregg_cell::compute_heap_root (Rust shadow of Lean Substrate.Heap.root, pinned by root_binds_get) at cell/src/state.rs:429, and dregg_cell::CellState is the heap. What umem adds over raw dregg_cell is a thin REUSABLE wrapper: the record-laying-into-collections convention + fork/checkpoint/restore(time-travel) that execution-lease currently reimplements inline. control/compute_cell.rs already treats dreggnet_umem::

### rental-economics (rental + auth + metering + accounts): of the 5 theme subdirs, 2 are MIGRATED (receipt -> kernel turn/BridgeReceipt discipline; webauth -> its credential core is a byte-for-byte port of the native, verified dregg-auth dga1_ scheme, with only edge/deploy HTTP glue remaining) and 3 are UNIQUE customer-facing cloud surfaces with no native equivalent yet — billing (invoices/spend-caps/estimation), org (teams/IAM), and guard (abuse quotas/takedown/standing). Crucially, none of the 3 UNIQUE crates needs new authority or metering primitives: they compose already-native, largely Lean-proven building blocks — cell::obligation_standing (recurring payment), cell::allowance (rate-bounded ceiling), dregg-auth attenuation (role-caps / quotas), tool-access-delegation (verified rate mandate), dregg-payable (value flow), and kernel turn receipts. The right dregg-native home for all three is new starbridge-apps/* factory cells alongside subscription/execution-lease/compute-exchange, turning operator-side Mutex ledgers into executor-enforced verified turns. The shared dreggnet-receipt/dreggnet-exec-budget deps that billing/guard/org lean on are themselves slop shims superseded by native turn receipts + cell::allowance.
- **receipt** `[MIGRATED]` · low — A std-only 'one product-wide receipt contract' (prev-hash-chained + ed25519-signed + re-witnessable attestations) that every DreggNet product crate seals its records into.
    - home: turn/src/turn.rs (TurnReceipt prev-hash chain, proven tamper-evident in metatheory Receipt.lean) + cell-crypto/src/note_bridge.rs (BridgeReceipt envelope)
    - port: Its own doc admits the kernel 'already has the right primitive twice over.' In the dregg-native world every product action IS a turn, so the kernel TurnReceipt is the receipt; no separate portability shim is needed. Safe to delete once its dependents (billing/guard/org) migrate to emitting real turn receipts.
- **webauth** `[MIGRATED]` · medium — dregg-cap forward-auth service (Caddy forward_auth calls /auth; valid dga1_ credential -> 2xx + identity headers, else 401) replacing legacy basic-auth on operator surfaces.
    - home: dregg-auth (src/credential/{chain,caveat,pred,wire}.rs — the dga1_ caveat-chain, machine-checked in metatheory/Dregg2); also credentials/ and macaroon/. discharge-gateway/ is the native standalone-HTTP dregg-cap-service pattern.
    - port: src/cred.rs is self-described as a 'faithful, wire-compatible, byte-for-byte port' of dregg-auth::credential — pure duplication of native, verified code, safe to delete. What remains (the /auth HTTP server, host->cap config, login flow) is edge/deploy glue, not a cell program; it should depend on dregg-auth's verify rather than the vendored port. No dregg-native cell logic is lost by deleting the slop copy.
- **billing** `[UNIQUE]` · high — Customer-facing billing plane over the existing meter: per-account invoices (receipt-anchored line items), DEC spending limits + budget alerts, and pre-deploy cost estimation.
    - home: starbridge-apps/ (new app cell, sibling of subscription + execution-lease); recurring-payment half already native as cell/src/obligation_standing.rs (StandingObligation, used by starbridge-apps/subscription/src/obligation.rs) and value flow via dregg-payable; spend-cap ceiling primitive native as cell/src/allowance.rs (rate-limited spending allowance).
    - port: A factory-born billing cell (like subscription/escrow-market). Invoice = an aggregation VIEW over already-settled turn receipts, each line item carrying the settle-receipt hash it was billed from, then sealed as its own turn receipt (no dreggnet-receipt shim). Spend cap = a cell::allowance ceiling cell — a charge over the cap is refused by the verified executor (the 402 shape), not by a trusted flag. Cost estimate = a pure function over the rate card (no cell state). Verified turns: assemble-inv
- **org** `[UNIQUE]` · high — Teams/organizations IAM: an Org is its own dregg cell/identity owning resources; members are dga1_ cap-accounts holding a role, where a role IS an attenuation of the org-owner authority; invite/accept/remove/transfer are
    - home: starbridge-apps/ (new app cell). Reuse dregg-auth attenuation (dregg-auth/src/grant.rs + credential chain, provably no-amplify) for role-caps; pattern on starbridge-apps/tool-access-delegation (attenuated mandate cell) and starbridge-apps/governed-namespace (governance-bound turns). No native org/team/tenant crate exists (searched whole workspace).
    - port: An Org factory cell owning member->role slots (SetField / WriteOnce), where role-cap = an org-owner dregg-auth grant attenuated to the role's permission set — so 'viewer is read-only' / 'admin cannot delete the org' is the SAME no-amplify lattice property dregg-auth already proves. Verified turns: invite, accept (mints the member role-cap), remove, change-role, transfer-ownership — each a real kernel turn receipt (the crate's own in-record MembershipEvent log becomes the turn chain). Org-scoping
- **guard** `[UNIQUE]` · medium — Per-account abuse-prevention admission gate for a permissionless cloud: per-account quotas, deploy/request rate-limiting, receipted abuse-takedown, and account standing (good/flagged/suspended).
    - home: starbridge-apps/ (new app cell). Rate-limit + quota ceilings are already native: cell/src/allowance.rs (rate-limited, per-epoch-refilling spending allowance = the rate-bounded ceiling) and starbridge-apps/tool-access-delegation (verified Monotonic-counter + WriteOnce-ceiling rate mandate, Lean-mirrored). The takedown/standing/governance flow has no native home; pattern it on starbridge-apps/govern
    - port: Compose existing native primitives rather than the hand-rolled Mutex ledgers: per-subject quota/rate = cell::allowance ceiling cells (a resource over ceiling refused in-band = 402/429), reusing tool-access-delegation's verified rate primitive for the counter+ceiling teeth. Account standing = a SetField slot moved only by a verified governance turn; a suspension/takedown = a receipted governance turn (a real kernel turn receipt, replacing the dreggnet-receipt dependency). Only the abuse-governanc

### web-serving — the DreggNet web edge + management console. These are the OLD proprietary DreggNet edge: a stack of hand-rolled pure-std thread-per-connection HTTP/1.1 servers serving HTML/JSON (landing, console, webauth forward-auth, gateway) plus the agent-served-web-app router (webapp) and a clean-room HTTP value vocabulary (http). The dregg-native direction is a categorically different web model: pages are dregg:// cells served/verified through the membrane (starbridge-web-surface::web_of_cells, deos-web-cells), UI is deos-js applet view-trees rendered to HTML/gpui by deos-view (the portable IR), and the public front door is static files under site/ served by Caddy. So most of this theme is either already covered by a native crate (landing→site/, webauth-cred→dregg-auth) or is a traditional-HTTP realization whose concept has a native home (webapp/gateway hosting → execution-lease + web-of-cells). The genuinely unique survivors are narrow compatibility surfaces (the fly-Machines API) and the unshipped console-as-IR.
- **landing** `[MIGRATED]` · low — Public landing page: one self-contained server-rendered HTML front door (what DreggNet Cloud is + live status embed + 3-step quickstart), served by a bespoke pure-std thread-per-connection HTTP server.
    - home: site/root/index.html (+ site/assets/style.css, site/deos/, site/deos-viewer/) — the static public front door already exists there, on the shared green design tokens, served by Caddy as static files.
    - port: The front-door role is already covered by the static site/ pages. Fold any unique landing copy (LandingConfig pitch text, the /status.json live-banner enhancement) into site/root/index.html, then the dreggnet-landing crate (config.rs + render.rs + the pure-std HTTP bin) is redundant and safe to delete — the native model serves static HTML from site/, not from a hand-rolled per-connection server.
- **console** `[UNIQUE]` · high — Signed-in customer console: the authenticated 'my stuff' home that aggregates a subject's OWN sites/servers/agents/domains/storage/$DREGG, cap-scoped to the webauth dga1_ subject, with in-page verify-don't-trust; a pure-
    - home: deos-view (the portable view-tree IR renderer: deos-view/src/web.rs HTML projection + deos-view/src/render.rs gpui) for the surface; dregg-auth (dregg-auth/src/credential) for the subject/cap scoping. This is exactly the 'console-as-IR proto in flight'.
    - port: Express the console as a deos-js applet view-tree (a vstack of the subject's cells — each resource row a bind() over the live ledger, each action a cap-gated verified turn), rendered by deos-view (browser via web.rs, or in-cockpit via render.rs) — no bespoke HTML server. Cap-scope by the dregg-auth caveat-chain subject rather than console/src/scope.rs's fixture catalog. Named product gap (docs/WEB-PORTALS-CENSUS.md 'no unified console'), so high priority; smallest shape is one deos-view applet c
- **webauth** `[UNIQUE]` · medium — dregg-cap forward-auth for the web edge: Caddy forward_auth calls /auth once per request; a valid dga1_ ed25519 caveat-chain credential (verified + attenuation-checked against the surface's required capability) returns 2
    - home: dregg-auth (dregg-auth/src/credential/{chain,caveat,wire,pred}.rs) — its credential core (cred.rs) is EXPLICITLY 'a faithful, wire-compatible port of breadstuffs dregg-auth::credential', i.e. a redundant fork that must become a dependency (that half is effectively MIGRATED).
    - port: Delete webauth/src/cred.rs and depend on dregg-auth for the ed25519 caveat-chain verify + attenuation. What remains truly unique is a THIN forward-auth decision: the grant vocabulary (grant.rs: ops-admin/grafana-view/gateway-admin host→cap map) + the /auth endpoint (server.rs). In the pure web-of-cells model caps enforce at the cell membrane, not at an HTTP edge, so this Caddy shim is a bridge-to-legacy-deploy artifact — port only the decision seam over dregg-auth, or retire it once membrane-enf
- **webapp** `[UNIQUE]` · medium — Agent-served web apps: an agent declares a WebApp (routes binding method+path → owned-wasm handlers); Router::serve matches a request, builds the handler's WorkloadSpec, runs it on the owned sandbox (LeasedRouter as a du
    - home: starbridge-apps/execution-lease (starbridge_execution_lease — the metered StandingObligation + Payable + durable umem cell-heap image lease is ALREADY the native migration of this crate's lease/meter half) + deos-web-cells / starbridge-web-surface::web_of_cells (publish/fetch a web bundle AS a dregg:// cell) for the served-app + static-hosting halves.
    - port: The lease/meter/durable-image half is DONE natively in execution-lease — LeasedRouter's funded-lease gate maps onto its per-period obligation + Payable transfer. The remaining unique piece (route → owned-wasm handler → HTTP response) becomes, in the native model, a deos-web-cells WebBundle (WebOfCells::publish/fetch/rehydrate) or a deos-view applet cell rather than an HTTP router: the 'app' is a published cell served/verified through the membrane. Smallest shape: a small crate composing executio
- **gateway** `[UNIQUE]` · medium — The runnable HTTP edge binary: a std TcpListener thread-per-connection loop multiplexing a fly.io-compatible Machines API (POST/GET/DELETE /v1/apps/{app}/machines[...] → create/list/status/stop/start/destroy, each mapped
    - home: starbridge-apps/execution-lease (the lease each machine-create maps onto — already native) + deos-web-cells / starbridge-web-surface::web_of_cells (the SiteHostHandler/sitepublish static hosting = web-of-cells publish/fetch). No native fly-Machines API exists anywhere in the workspace (grep of starbridge-apps/deos-view/starbridge-web-surface: none).
    - port: The genuinely-unique, no-native-equivalent piece is the fly.io Machines HTTP compatibility surface (route.rs + lease.rs + api.rs): a thin adapter binding fly routes → execution-lease turns. The other handlers are conceptually MIGRATED — SiteHostHandler/hosting.rs/sitepublish.rs → WebOfCells::publish/fetch on dregg:// cells; WebAppHandler → the webapp entry above; webauth gating → dregg-auth. Smallest native shape: IF fly-compat is a product requirement, keep only a small fly→execution-lease adap
- **http** `[UNIQUE]` · low — Clean-room dependency-free HTTP/1.1 value vocabulary — Method, StatusCode, Request, ResponseWriter (slice-backed buffer append), Handler/HandlerResult — the pure-std types the gateway/console/landing/webauth serving loop
    - home: No native http-types crate exists; it is pure infrastructure for exactly the four traditional-edge crates above (gateway/console/landing/webauth all depend on it). Closest native serving code is starbridge-v2/web (pty_ws.rs), which uses websockets over wasm panes, not this vocabulary.
    - port: Trivial (4 files: handler/request/response). It has no native equivalent only because the native web model doesn't hand-roll HTTP/1.1 at all (web-of-cells + deos-view HTML strings + Caddy-served static site/). It is DEAD-adjacent: it survives exactly as long as its consumers do. Port only if a traditional HTTP edge is deliberately retained (then fold it into the surviving fly-adapter/gateway crate as a private module); otherwise it dies with gateway/landing/console/webauth.

### bridges (integration bridges: grains=cells, ipfs, domains, lease-weld, web attach)
- **bridge** `[MIGRATED]` · high — The keystone weld: a funded dregg execution-lease is mapped to a CapTier and drives a durable, exactly-once, crash-resumable metered workflow (duroxide + owned wasmi sandbox), budget-gated so an over-budget MeterTick lap
    - home: starbridge-apps/execution-lease/src/{lib,service}.rs (+ starbridge-apps/compute-exchange)
- **attach** `[MIGRATED]` · medium — Portal web attach — a signed-in (webauth dga1_) user drives THEIR OWN cap+budget+receipt-bounded hosted agent session in the browser: NL goal -> live reason/act/observe SSE transcript -> budget meter + accumulating recei
    - home: dregg-agent/src/session.rs (the hosted 'SSH/portal attach unit') + dregg-agent/src/live.rs (LiveRun + verify_live) + dregg-agent/src/session_store.rs; desktop face in starbridge-v2/src/deos_desktop/{agent_room,attach_wizard,hireling}.rs
- **sandstorm-bridge** `[UNIQUE]` · low — Run Sandstorm .spk apps as dregg cells (grain = cell + cap-bounded workload): real .spk xz/Ed25519/Cap'n-Proto parsing, manifest->GrainSpec, grain /var = cell umem heap, a WebSession HTTP bridge shim with X-Sandstorm-* h
    - home: cell/ (grain /var = a cell umem heap + content-addressed DataRoot) + dregg-agent/exec for the cap-bounded workload; powerbox=caps already exists in dregg-agent/src/grant.rs and starbridge-v2/src/powerbox.rs
    - port: A small crate for the .spk verify + manifest->GrainSpec decode (pure, no net); the grain lifecycle (create/wake/sleep/delete) as a cell over cell/ with the /var heap folded into the state commitment; the powerbox grant as a strictly-attenuating witnessed turn reusing the existing cap grant machinery — no new verified primitive, it reuses cell + agent caps.
- **dregg-ipfs** `[UNIQUE]` · low — IPFS transport bridge: a dregg blake3 content commitment IS an IPFS CIDv1 (blake3 multihash, raw codec), so content-addressed dregg objects/site assets/merge deltas are pinned + fetched over IPFS (injected IpfsClient sea
    - home: storage/ (storage/src/content.rs + storage/src/commitment.rs already blake3 content-address everything; storage/src/retrieval.rs is the fetch surface)
    - port: A transport-only module in storage/: the CIDv1 encode/decode over the existing blake3 commitment (no re-hash — Cid::from_blake3_digest wraps the commitment) plus the injected IpfsClient/HttpPost seam. No verified turns needed — the commitment is already the address; only the transport/pinning is new, gated behind the seam exactly like the existing retrieval trait.
- **dregg-domains** `[UNIQUE]` · medium — BYO custom domains for hosting: a cap-gated DomainBinding cell (domain, bound site, owner, verification state), an ACME-style DNS proof-of-control (TXT _dregg-verify.<d>=nonce OR CNAME) resolved through an injected DnsRe
    - home: starbridge-apps/nameservice/src/lib.rs (the FactoryDescriptor exemplar for a naming cell) — but DNS-challenge BYO domains are distinct from federation names; the hosting SiteCell it binds to is itself still dreggnet-side (dreggnet-webapp::hosting)
    - port: A new starbridge-app: a FactoryDescriptor cell with slots {domain, site, owner, verification_state, challenge_nonce}, a DomainCap-gated bind turn + a verify turn (WriteOnce(challenge_nonce), Monotonic(verified_seq) so who-proved-control-when is re-witnessable), the DnsResolver as an injected seam (MockDns in tests). site_for_host / is_verified become verified reads the gateway consults.

### ops-deploy — DreggNet's operations/deployment/CLI surface. Ground truth: this theme is almost entirely DreggNet-cloud-deployment INFRA (Docker/Postgres/Caddy/systemd-fleet tooling + operator CLI + ops dashboard + runbooks), which is DEAD/deletable — it targets the specific deployed stack being retired, and the main workspace already has its own docker/, scripts/, and cli/. Two crates are the exception: dregg-deploy (git-push-to-deploy durable workflow) and dregg-secrets (managed KMS + cap-gated sandbox injection) are genuinely UNIQUE app logic with NO native equivalent and should port into starbridge-apps/* on app-framework + execution-lease. CRITICAL namespace-collision caveat verified against the root Cargo.toml members: main-workspace dregg-deploy (DreggDL capability-layout DSL), secrets/dregg-secrets (local AES/keychain store), cli/dregg-cli (federation client), and observability/dregg-observability (trace emitter) are DIFFERENT crates from the same-named dreggnet ones — do NOT treat the name match as MIGRATED. No 'probes' subdir exists in dreggnet.
- **dregg-deploy** `[UNIQUE]` · medium — git-push-to-deploy PaaS: clone -> detect -> cap-bounded build -> publish a SiteCell, as a crash-resumable, exactly-once, metered duroxide durable workflow (Clone/Build/Publish/MeterTick activities), with the source commi
    - home: No native equivalent. NOTE the name collides: main-workspace dregg-deploy (/Users/ember/dev/breadstuffs/.claude/worktrees/deos-night-shift/dregg-deploy) is a totally DIFFERENT thing — DreggDL, a CapDL-inspired declarative capability-layout DSL that lowers to a dregg_turn::CallForest and checks it with dregg-userspace-verify. It is NOT git-deploy. No starbridge-apps/* crate covers build-and-publish
    - port: New starbridge-apps/site-deploy crate (~1 crate, mirrors execution-lease's shape: Cargo.toml + manifest.json + src + tests). Model the deploy as a factory-born durable cell whose one-way lifecycle Clone->Build->Publish is verified turns enforced by the real executor. Reuse starbridge-apps/execution-lease's StandingObligation per-period meter (cell/src/obligation_standing.rs) + Payable Transfer (app-framework/src/payable.rs) for the metered build lease; the build image is the committed umem cell-
- **dregg-secrets** `[UNIQUE]` · medium — managed secrets / KMS for workloads: cap-account-owned AEAD-at-rest store (XChaCha20-Poly1305 envelope under a KMS-root->per-account->per-secret BLAKE3 key hierarchy), a cap-gated runtime injection seam that delivers a s
    - home: The managed/cap-gated/injection/audit layer has no native equivalent (grep of starbridge-apps for secret/kms/inject returned nothing). PARTIAL overlap only on the storage primitive: main-workspace secrets/ (/Users/ember/dev/breadstuffs/.claude/worktrees/deos-night-shift/secrets, crate dregg-secrets) is a single-user LOCAL store — AES-256-GCM file + OS keychain behind a SecretStore trait — not a mu
    - port: The unique parts to port: (1) THE INJECTION SEAM (inject.rs, ~256 lines) — inject_for_workload resolves declared SecretBinding{Env|File} through a cap-gated read, returns a redacted-Debug InjectedSecrets with redaction_set/scrub, fail-closed if any binding is refused; this generalizes the BYO-LLM-key brain-pocket confinement (deos-hermes resident.rs, DEOS-NIGHT-SHIFT item 9) so it belongs beside the native exec/sandbox seam. (2) THE RECEIPTED AUDIT (audit.rs) — model secret reads as verified tur
- **ops** `[DEAD]` · low — admin-only ops/observability dashboard (crate dreggnet-ops): a pure-std thread-per-connection HTTP server that AGGREGATES the live DreggNet cloud read surfaces (dregg node :8420, gateway :8080, discord bot :8080), reads 
    - home: Superseded, not migrated. It is 100% coupled to the specific deployed DreggNet stack being retired (scrapes Docker Engine API, compose-internal Postgres meter, Caddy edge, the :8420/:8080 HTTP surfaces). Note the collision: main-workspace observability/ (crate dregg-observability) is unrelated — a trace-event emitter for the turn substrate, not an ops dashboard. The dregg-native replacement is the
- **cli** `[DEAD]` · low — the dregg-cloud operator CLI (crate dreggnet-cli, bin dregg-cloud + dreggnet-crash-resume): a thin face over the DreggNet control plane — lease open/run/status, deploy <repo>, domains add/verify, logs, login — routing ev
    - home: The crate is a thin weld with no logic of its own; it is deletable and its home is the existing native CLI. Collision note: main-workspace cli/ (crate dregg-cli, bin dregg, /Users/ember/dev/breadstuffs/.claude/worktrees/deos-night-shift/cli) is the dregg-native command-line client (identities, tokens, node interaction) and is dist-shipped. The dregg-cloud VERB SURFACE (lease/run/deploy/logs/domain
- **deploy** `[DEAD]` · low — physical compute-fabric deployment infra: systemd-unit node agents (node-agent binds 0.0.0.0:8021 and answers POST /fulfill from the edge mesh-dispatch over headscale/WireGuard, running a lease as a real durable metered 
    - home: Bare-metal/VM fleet + systemd + overlay-network deployment infra for the DreggNet compute backend that is being retired — not dregg-native app logic. Per DREGG-COMPUTER.md the native 'vat substrate' is ServerFleet/ServerRecord CELLS (one cap grammar, one witness discipline, one settlement rail), not systemd node-agents POSTing JSON over WireGuard. Deletable; nothing here is dregg-native shape. If 
- **Makefile** `[DEAD]` · low — the DreggNet repo build/test/lint driver: defines the offline-green SERVICE_CRATES set (dreggnet-cli/durable/exec/bridge/control/webapp/ops), env-gated lanes (test-pg needs DATABASE_URL, test-verify needs a live node, te
    - home: Build tooling scoped entirely to the dreggnet-* crate set (a workspace that is itself in the main repo's exclude list). The main workspace has its own build/test tooling — /Users/ember/dev/breadstuffs/.claude/worktrees/deos-night-shift/scripts/ (test-gauntlet.sh, ship-readiness-smoke.sh, devnet-smoke.sh, git hooks) — and its own docker/ dir. Deletable with the dreggnet workspace; no port.
- **docker** `[DEAD]` · low — DreggNet container/compose infra (root Dockerfile + docker-compose.yml + .dockerignore, no docker/ subdir): a Postgres-16 durable/meter substrate service, the dregg-cloud operator-CLI image (wasmi-tier exec demo), and th
    - home: DreggNet-repo-specific packaging that builds dreggnet-cli/gateway against Postgres. The main workspace already has its own container infra at /Users/ember/dev/breadstuffs/.claude/worktrees/deos-night-shift/docker/ (Caddyfile, Dockerfile, Dockerfile.node, docker-compose.yml, devnet-config, start-devnet.sh, build-multiarch.sh). Deletable; nothing to migrate.
- **scripts** `[DEAD]` · low — scripts/test.sh — the offline-green test-gauntlet driver behind `make test`: runs the portable service-crate stack (matching CI's macOS job) and reports the env-gated Postgres/live-node/Linux lanes as SKIPPED (never fail
    - home: One shell driver for the dreggnet crate set. Superseded by the main workspace's scripts/ gauntlet (/Users/ember/dev/breadstuffs/.claude/worktrees/deos-night-shift/scripts/test-gauntlet.sh + ship-readiness-smoke.sh). Deletable.
- **runbooks** `[DEAD]` · low — operational runbooks for running the deployed DreggNet cloud: DEPLOY (edge AWS box + Caddy + DNS), DISASTER-RECOVERY, INCIDENT-RESPONSE, KEY-MANAGEMENT, SECRETS, STRIPE-OPS/SETUP, FEDERATION, MESH, NODE-OPS, UPGRADE, COM
    - home: Prose ops docs bound to the specific retiring deployment (the AWS edge box, docker compose, Caddy certs, Stripe account, headscale mesh, node fleet). Not code, no dregg-native equivalent needed. Deletable as a set; a few pattern docs (KEY-MANAGEMENT.md, DISASTER-RECOVERY.md, INCIDENT-RESPONSE.md) are worth salvaging into the main /docs/ tree if the cloud is ever re-stood-up, but they are reference

### misc-remainder
- **receipt** `[MIGRATED]` · low — The one product-wide receipt contract: prev-hash-chained + ed25519-signed receipts, a shared re-export of the kernel receipt discipline.
    - home: turn/src/lib.rs (TurnReceipt / BridgeReceipt, verify_receipt_chain) — the chain+ed25519 primitive already lives in the native `turn` crate; dreggnet-receipt is a thin unifying view over it.
- **storage** `[MIGRATED]` · medium — Durable object storage where a bucket is a dregg cell and objects are content-addressed, cap-gated, metered, receipted turns.
    - home: storage/ (dregg-storage cell-program templates) + dregg-storage-templates/ + starbridge-apps/storage-gateway-mandate (StorageOp GET/PUT/LIST, prefix auth, volume budget) — object storage as cell-programs is already migrated; only the tenant bucket-API veneer would re-home.
- **umem** `[MIGRATED]` · low — A registry primitive where a registry IS a umem cell, persisted/reconstructed via heap commit/restore over the real sorted-Poseidon2 heap root.
    - home: turn/ (umem cell tests + continuation heap commit/restore) + cell/src/*.rs compute_heap_root; the verified-KV-register-as-service-cell equivalent is starbridge-apps/kvstore. dreggnet-umem is itself the substrate-native rewrite of the old dreggnet-store and duplicates the native umem substrate.
- **dregg-secrets** `[MIGRATED]` · medium — Managed secrets/KMS: AEAD-at-rest per-account secret store with cap-gated runtime injection, versioned rotation, receipted access audit.
    - home: secrets/ (Dregg Secret Store — AES-256-GCM encrypted store, pluggable backends) + macaroon/ + credentials/. The KMS root->per-account->per-secret envelope hierarchy and the never-logged injection seam are the unique delta to fold onto `secrets`, not a new crate.
- **webauth** `[MIGRATED]` · low — Dregg-cap forward-auth at the Caddy edge: forward_auth calls /auth, a valid dga1_ caveat-chain returns 2xx else 401.
    - home: dregg-auth/ (scoped/time-boxed/delegatable/revocable offline-verifiable cap verification) already IS the verification core; dreggnet-webauth is the thin Caddy HTTP-edge glue over it. Only the forward_auth HTTP shim is non-native.
- **dregg-deploy** `[UNIQUE]` · high — Auto-deploy-from-git: clone -> detect -> cap-bounded build -> publish a site cell, as a crash-resumable metered durable workflow.
    - home: NAME COLLIDES with the main `dregg-deploy` but that is DreggDL (declarative CapDL-style layout, apply/diagnose/facet) — completely different functionality, NOT a migration. Closest native home: the durable-workflow tree + exec sandbox (compute-exchange lease tier).
    - port: A durable workflow crate (duroxide-style) whose steps (clone/detect/build/publish) are verified turns checkpointed to a receipt log, mirroring starbridge-apps/agent-orchestration's crash-recoverable exactly-once workflow shape; the site-publish step targets the storage-as-cell templates.
- **guard** `[UNIQUE]` · high — Per-account abuse-prevention layer (rate/quantity limits, sybil resistance) for a KYC-free permissionless cloud.
    - home: No native equivalent. Closest home: a cap-gated policy cell in starbridge-apps (mandate/budget pattern) or fold onto the control/compute-exchange admission path.
    - port: A FactoryDescriptor + CellProgram whose per-account rate/quota state is a conserved-budget tooth (like swarm-orchestration's conserved budget), enforced as a fail-closed pre-check turn refused before the guarded action runs.
- **billing** `[UNIQUE]` · high — Invoices + spending limits + cost estimation over the meter rail; each invoice line traces to settled meter receipts and is sealed into the receipt chain.
    - home: No native invoice/billing crate. Closest home: extend dregg-payable/ (the payable/metering surface) or a new starbridge-app cell; receipt-anchoring already rides native `turn` receipts.
    - port: An invoice-as-cell FactoryDescriptor: line items are SetField turns over aggregated meter-receipt hashes, the sealed invoice is an EmitEvent turn into the receipt chain — a verifiable bill re-witnessable against the meter receipts it billed from.
- **webapp** `[UNIQUE]` · high — Agent-served web apps: an agent declares routes -> owned-wasm handlers, run on the sandbox under a lease and served over HTTP.
    - home: No native equivalent for the route->wasm-handler serving model. Closest home: dreggnet/exec (owned wasmi sandbox) on the compute-exchange/execution-lease tier + starbridge-web-surface for the cap-confined fetch side.
    - port: A cell whose declared routes are cap-gated affordances; each request is a verified turn dispatching to a lease-metered wasm handler run via the exec sandbox seam, receipted per request.
- **org** `[UNIQUE]` · medium — Teams/orgs IAM: an org is its own cell that owns resources, members are cap-accounts holding roles, a role is an attenuation of org-owner authority; receipted membership turns.
    - home: Strong native substrate exists: starbridge-apps/identity (verifiable credentials), governed-namespace, and tool-access-delegation already express role-as-cap-attenuation. Closest home: compose those rather than a standalone crate.
    - port: An Org FactoryDescriptor owning resource cells; invite/accept/remove/transfer are receipted SetField/EmitEvent turns; a role is a cap grant with granted-subset-of-held attenuation checked by the executor (the non-amplification tooth from agent-orchestration).
- **dregg-domains** `[UNIQUE]` · medium — BYO custom domains: a DomainBinding cell, DNS TXT/CNAME challenge-verify, and verified custom-Host gateway routing to a published site cell.
    - home: The naming/cell primitive is native (directory/ register-lookup-revoke, starbridge-apps/nameservice). Closest home: build the binding cell on directory/nameservice; the DNS-challenge + custom-Host gateway routing is the unique delta.
    - port: A DomainBinding FactoryDescriptor over the directory primitive; challenge-verify is a turn that witnesses the DNS TXT/CNAME proof, then flips the binding to verified so the gateway routes custom Host to the site cell.
- **dregg-ipfs** `[UNIQUE]` · low — IPFS bridge: a dregg blake3 content commitment IS an IPFS CIDv1, so content-addressed objects pin/fetch over IPFS and re-witness against the same address.
    - home: No native IPFS bridge. Closest home: a net-free verified core with an injected transport seam, exactly like bridge/ and dregg-ipfs already do (IpfsClient trait).
    - port: Keep the verified core net-free (blake3<->CIDv1 multihash identity is a pure fn); the IPFS transport stays an injected `IpfsClient` seam — one small crate, no new verified turns beyond the re-witness check.
- **dreggnet-logs** `[UNIQUE]` · medium — Per-tenant runtime logs: capture, durable store, cap-scoped tail/follow/search.
    - home: observability/ (studio-shape trace event emitter for the turn substrate) is the natural home for capture/emit; the cap-scoped durable tail/follow/search store has no native equivalent yet.
    - port: Extend observability's emitter with a per-tenant durable sink; tail/follow/search are cap-scoped read turns over that sink (re-witnessable against the tenant cell), rather than a standalone crate.
- **gateway** `[UNIQUE]` · medium — The fly.io-compatible machines HTTP API — the public control surface for scheduling/lifecycle.
    - home: Overlaps the compute-exchange / control migration (rentable-machine provider). Closest home: starbridge-apps/compute-exchange + the control plane; net/node for HTTP serving.
    - port: A thin HTTP adapter mapping fly-machines verbs onto compute-exchange cell operations (each machines call -> a cap-gated lease/schedule turn); no domain logic of its own once control is native.
- **http** `[DEAD]` · low — A small HTTP/1.1 value vocabulary (request/response/handler types, ~555 LOC) the gateway serves through.
    - home: Generic reinvented HTTP types with no dregg semantics; superseded once the gateway is a thin adapter (see gateway entry). Fold into the gateway adapter or use net/ — do not port as its own crate.
- **cli** `[UNIQUE]` · low — The DreggNet operator+developer CLI (open/fund a lease, run a metered durable workload, check status) over the control plane.
    - home: Product-specific verbs; the main `cli/` crate is the dregg CLI home. Closest home: add the lease/durable/status subcommands to cli/ once control+compute-exchange are native.
    - port: Subcommands are thin clients over the migrated control/compute-exchange cell operations — no logic beyond arg-parsing and turn dispatch; retire once those backends land.
- **console** `[UNIQUE]` · low — Signed-in customer console — the 'my stuff' home to see/manage a user's own sites/servers/agents/domains/storage/$DREGG, cap-scoped with in-page verify.
    - home: Presentation surface over the (migrating) backends; no native equivalent. Closest home: the deos web tree — deos-leptos / starbridge-web-surface.
    - port: A Leptos/web-surface view whose reads are cap-scoped verified turns against the user's cells; carries no backend logic, so it lands after the backends it renders are native.
- **landing** `[UNIQUE]` · low — Public marketing landing page (what DreggNet is, live status embed, three-step quickstart).
    - home: Static-ish marketing surface. Closest home: the site/ tree or deos-leptos; near-DEAD for migration purposes.
    - port: A static page + a client-side status embed; no dregg-native logic — lowest-value port, or serve directly from site/.
- **status** `[UNIQUE]` · low — Public no-auth status page — overall/per-service health, the n=4 federation panel, incidents/uptime, and /status.json.
    - home: The public subset of ops/. Closest home: deos-leptos/site web tree; health data comes from the node/federation daemon (node/).
    - port: A read-only view over node/federation health turns + a machine-readable /status.json projection; no state of its own.
- **ops** `[UNIQUE]` · low — Operator/admin dashboard aggregating node/gateway/durable/bot surfaces into one admin-gated pane.
    - home: Admin presentation over migrating backends; no native equivalent. Closest home: deos web tree, admin-gated via dregg-auth cap.
    - port: An admin-cap-gated aggregation view over the other surfaces' read turns; lands after those backends are native (status/ is its public subset).
- **sandstorm-bridge** `[UNIQUE]` · low — Sandstorm integration: grain=cell, .spk runner, powerbox=dregg-cap bridge, welded to the exec/webauth/webapp surfaces.
    - home: Niche integration with no native equivalent; depends on exec/webapp/webauth (themselves migrating). Closest home: keep as an integration crate over the native exec sandbox + cap surfaces.
    - port: A thin adapter mapping a Sandstorm grain onto a dregg cell and powerbox requests onto cap grants, running the .spk via the native exec sandbox seam — experimental, port last.
- **demo** `[DEAD]` · low — Demo shell scripts (stripe-trigger/receiver, crash-resume, run-demo).
    - home: Throwaway demo/hackathon tooling, not product code — no migration; delete.
- **deploy** `[DEAD]` · low — Deployment scaffolding: markdown backend/federation docs plus node-agent, persvati-agent, observability, and staging configs.
    - home: Ops/deploy scaffolding + docs; superseded by the runbooks and the native deploy tooling. The one live-code piece is deploy/node-agent (a systemd node agent) — if any node-provisioning logic is unique it re-homes onto node/, otherwise deletable.
- **runbooks** `[DEAD]` · low — Operator runbooks (deploy, DR, incident, key mgmt, mesh, stripe ops, upgrade) as markdown.
    - home: Pure documentation — move to docs/ if still accurate, otherwise delete; nothing to port.
- **scripts** `[DEAD]` · low — A single test.sh helper.
    - home: Throwaway build/test tooling; no migration — delete.

